{
  "private": true,
  "scripts": {
    "approve": "backstop approve --config backstop.config.js",
    "clear": "rimraf backstop_data/bitmaps_test backstop_data/html_report",
    "reference": "run-s clear _reference",
    "_reference": "backstop reference --config backstop.config.js $([ -z \"${CI+x}\" ] && printf '%s' '--docker')",
    "test": "run-s clear _test",
    "_test": "backstop test --config backstop.config.js $([ -z \"${CI+x}\" ] && printf '%s' '--docker')"
  },
  "devDependencies": {
    "backstopjs": "^5.0.1",
    "npm-run-all": "^4.1.5"
  }
}
