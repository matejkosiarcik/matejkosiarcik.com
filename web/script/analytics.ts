// TODO: fetch fallback/polyfill?

// (async () => {
//     try {
//         const host = process.env['NODE_ENV'] === 'production' ? 'http://localhost:6789' : 'http://localhost:6789'
//         await fetch(`${host}/matejkosiarcik-com/us-central1/analytics`)
//     } catch (error) {
//         console.error(`Could not reach analytics. Error: ${error}`)
//     }
// })()
