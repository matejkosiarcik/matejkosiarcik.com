{
  "private": true,
  "scripts": {
    "test": "backstop test --config backstop.config.js --docker",
    "pretest": "run-s prepare",
    "reference": "backstop reference --config backstop.config.js --docker",
    "prereference": "run-s prepare",
    "prepare": "backstop init"
  },
  "devDependencies": {
    "backstopjs": "^5.3.2",
    "npm-run-all": "^4.1.5"
  }
}
