{
  "private": true,
  "main": "build/index.js",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "lint": "run-p lint:*",
    "lint:es": "eslint . || true",
    "lint:ts": "tslint --project tsconfig.json",
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "firebase emulators:start --only functions,database",
    "prestart": "run-s build",
    "start": "run-p watch serve",
    "deploy": "run-s build deploy:firebase",
    "deploy:firebase": "firebase deploy --only functions,database"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.1",
    "firebase-tools": "^8.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "firebase-functions-test": "^0.2.0",
    "npm-run-all": "^4.1.5",
    "tslint": "^6.1.3",
    "typescript": "^3.8.0"
  }
}
