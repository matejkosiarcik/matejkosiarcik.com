{
    "private": true,
    "dependencies": {
        "apache-server-configs": "^2.0.0",
        "normalize.css": "^7.0.0"
    },
    "devDependencies": {
        "autoprefixer": "*",
        "mkdirp": "*",
        "node-sass": "*",
        "nps": "*",
        "onchange": "*",
        "postcss-cli": "*",
        "rimraf": "*"
    },
    "scripts": {
        "watch": "npm run build && onchange 'sources/**/*' --wait -- npm run build",
        "build": "nps build",
        "postinstall": "cd 'node_modules/normalize.css' && if [ -f 'normalize.css' ]; then mv 'normalize.css' '_normalize.scss'; fi"
    }
}
