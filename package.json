{
    "private": true,
    "dependencies": {
        "apache-server-configs": "^2",
        "normalize.css": "^7"
    },
    "devDependencies": {
        "autoprefixer": "*",
        "concurrently": "*",
        "mkdirp": "*",
        "mustache": "*",
        "node-sass": "*",
        "nps": "*",
        "onchange": "*",
        "postcss-cli": "*",
        "rimraf": "*",
        "typescript": "^3"
    },
    "scripts": {
        "build": "nps build",
        "clean": "rimraf 'build' 'public'",
        "dist": "nps dist",
        "postinstall": "cd 'node_modules/normalize.css' && if [ -f 'normalize.css' ]; then mv 'normalize.css' 'normalize.scss'; fi",
        "watch": "nps watch"
    }
}
