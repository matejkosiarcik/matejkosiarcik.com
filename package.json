{
    "private": true,
    "dependencies": {
        "apache-server-configs": "^3.2.1",
        "normalize.css": "^8.0.1"
    },
    "devDependencies": {
        "autoprefixer": "^9.6.1",
        "concurrently": "^4.1.2",
        "mkdirp": "^0.5.1",
        "mustache": "^3.0.1",
        "nps": "^5.9.5",
        "onchange": "^6.0.0",
        "postcss-cli": "^6.1.3",
        "rimraf": "^3.0.0",
        "sass": "^1.22.10",
        "webpack": "^4.39.2",
        "webpack-cli": "^3.3.7"
    },
    "scripts": {
        "build": "nps build",
        "clean": "rimraf 'build'",
        "deploy": "sh '$(PWD)/utils/deploy.sh'",
        "dist": "nps dist",
        "serve:dev": "cd 'build/dev' && browser-sync start --server --files '**/*'",
        "serve:prod": "docker-compose up",
        "start": "concurrently 'npm run watch' 'npm run serve:dev'",
        "watch": "nps watch"
    }
}
