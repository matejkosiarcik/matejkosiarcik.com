language: node_js

node_js:
  - node  # latest

build:
  ci:
    - apt-get update --yes
    - DEBIAN_FRONTEND=noninteractive apt-get install --yes ruby ruby-dev pandoc zlib1g zlib1g-dev imagemagick
    - make
