<?php
$user_url = $_SERVER["REQUEST_URI"];
$statuses = array(
    403 => "You don't have access to this resource",
    404 => "The requested URL \"$user_url\" was not found",
);

$status = $_GET["code"];
if (empty($status)) { $status = 404; } // default error
http_response_code($status);

$description = $statuses[$status];
if (empty($description)) { $description = "Unknown error occured"; }
?>

<!DOCTYPE html>
<html lang="en" class="no-js no-touch">
    <head>
        {{> head.html }}
        <link rel="stylesheet" type="text/css" href="/error/style.css">
    </head>
    <body class="font-size-m text-color-default">
        <div id="main-container">
            {{> header.html }}
            <div id="page" class="content clear">
                <div class="content">
                    <div class="status"> <?php echo $status; ?> </div>
                    <div class="description font-size-l">
                        <p> <?php echo $description; ?> </p>
                        <p> Back to <a class="content-link" href="/">home</a> </p>
                    </div>
                </div>
            </div>
            {{> footer.html }}
        </div>
    </body>
</html>
